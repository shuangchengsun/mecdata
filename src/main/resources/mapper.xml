<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ItemModel">
    <insert id="insertObject" parameterType="model.ItemModel">
        insert into oridata (userid,appname,longitude,latitude,startTime) values (#{userid},#{appname},#{longitude},#{latitude},#{startTime})
    </insert>
</mapper>